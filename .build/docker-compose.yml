name: portfolio

services:
  web:
    build:
      context: ..
      dockerfile: .build/Dockerfile
    image: "${IMAGE_NAME:-portfolio}:${IMAGE_TAG:-latest}"
    ports:
      - "${PORT:-8080}:80"
    develop:
      watch:
        - action: sync
          path: ../public
          target: /usr/share/nginx/html
